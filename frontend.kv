<CameraScreen>:
    GridLayout:
        cols: 1
        Camera:
            id: camera
            play: False
        Button:
            id: camera_button
            text: 'Start Camera'
            on_press: root.start() if root.ids.camera.play == False else root.stop()
        Button:
            text: 'Capture'

<RootWidget>:
    CameraScreen:
        id: camera_screen
        name: 'camera_screen'